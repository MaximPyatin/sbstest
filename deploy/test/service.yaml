apiVersion: v1
kind: Service
metadata:
  name: sbs-api
  labels:
    app: sbs-api
    environment: test
spec:
  type: ClusterIP
  selector:
    app: sbs-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http

